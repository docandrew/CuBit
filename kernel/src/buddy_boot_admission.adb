package body Buddy_Boot_Admission with SPARK_Mode is
   procedure Prove_Word_Bound (Item : Frame; Words : Word_Count) is
   begin
      null;
   end Prove_Word_Bound;

   procedure Prove_Whole_Tail (First, Last, Highest : Frame) is
   begin
      null;
   end Prove_Whole_Tail;

   procedure Prove_Bitmap_Bound (Item, Highest, Bitmap_Last : Frame) is
   begin
      null;
   end Prove_Bitmap_Bound;

   procedure Prove_Boundary (Highest : Frame) is
   begin
      null;
   end Prove_Boundary;
end Buddy_Boot_Admission;
