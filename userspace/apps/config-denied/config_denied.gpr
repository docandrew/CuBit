project Config_Denied extends "../config-check/config_check.gpr" is
   for Source_Dirs use (".");
   for Object_Dir use "build";
   package Builder is
      for Executable ("main.adb") use "config-denied.app";
      for Default_Switches ("Ada") use ("-nostdlib");
   end Builder;
end Config_Denied;
