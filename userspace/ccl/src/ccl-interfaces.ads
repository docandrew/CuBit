------------------------------------------------------------------------------
--  CuBit Control Language service interface declarations
--
--  Child packages are generated-style, immutable representations of canonical
--  service descriptors.  They contain type/effect identity only; publishing
--  one into a catalog never installs a runtime authority binding.
------------------------------------------------------------------------------
package CCL.Interfaces with
   Pure,
   SPARK_Mode => On
is
end CCL.Interfaces;
