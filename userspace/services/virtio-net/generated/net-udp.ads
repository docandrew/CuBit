------------------------------------------------------------------------------
--                                                                          --
--               Generated by RecordFlux 0.26.0 on 2026-03-04               --
--                                                                          --
--                     Copyright (C) 2018-2026 AdaCore                      --
--                                                                          --
--         SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception          --
--                                                                          --
------------------------------------------------------------------------------

pragma Ada_2012;
pragma Style_Checks ("N3aAbCdefhiIklnOprStux");
pragma Warnings (Off, "redundant conversion");
with Net.RFLX_Types;

package Net.UDP
with
  SPARK_Mode
is

   type Port is range 0 .. 2**16 - 1
   with
     Size =>
       16;

   use type Net.RFLX_Types.Base_Integer;

   function Valid_Port (Val : Net.RFLX_Types.Base_Integer) return Boolean is
     (Val <= 65535);

   function To_Base_Integer (Val : Net.UDP.Port) return Net.RFLX_Types.Base_Integer is
     (Net.RFLX_Types.Base_Integer (Val));

   function To_Actual (Val : Net.RFLX_Types.Base_Integer) return Net.UDP.Port is
     (Net.UDP.Port (Val))
   with
     Pre =>
       Valid_Port (Val);

   type Length is range 8 .. 2**16 - 1
   with
     Size =>
       16;

   function Valid_Length (Val : Net.RFLX_Types.Base_Integer) return Boolean is
     (Val >= 8
      and Val <= 65535);

   function To_Base_Integer (Val : Net.UDP.Length) return Net.RFLX_Types.Base_Integer is
     (Net.RFLX_Types.Base_Integer (Val));

   function To_Actual (Val : Net.RFLX_Types.Base_Integer) return Net.UDP.Length is
     (Net.UDP.Length (Val))
   with
     Pre =>
       Valid_Length (Val);

   type Checksum is range 0 .. 2**16 - 1
   with
     Size =>
       16;

   function Valid_Checksum (Val : Net.RFLX_Types.Base_Integer) return Boolean is
     (Val <= 65535);

   function To_Base_Integer (Val : Net.UDP.Checksum) return Net.RFLX_Types.Base_Integer is
     (Net.RFLX_Types.Base_Integer (Val));

   function To_Actual (Val : Net.RFLX_Types.Base_Integer) return Net.UDP.Checksum is
     (Net.UDP.Checksum (Val))
   with
     Pre =>
       Valid_Checksum (Val);

end Net.UDP;
