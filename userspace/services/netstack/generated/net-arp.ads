------------------------------------------------------------------------------
--                                                                          --
--               Generated by RecordFlux 0.26.0 on 2026-03-04               --
--                                                                          --
--                     Copyright (C) 2018-2026 AdaCore                      --
--                                                                          --
--         SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception          --
--                                                                          --
------------------------------------------------------------------------------

pragma Ada_2012;
pragma Style_Checks ("N3aAbCdefhiIklnOprStux");
pragma Warnings (Off, "redundant conversion");
with Net.RFLX_Types;

package Net.ARP
with
  SPARK_Mode
is

   type Hardware_Address_Space is (HAS_ETHERNET, HAS_HW_EXP1, HAS_HW_EXP2)
   with
     Size =>
       16;
   for Hardware_Address_Space use (HAS_ETHERNET => 1, HAS_HW_EXP1 => 36, HAS_HW_EXP2 => 256);

   use type Net.RFLX_Types.Base_Integer;

   function Valid_Hardware_Address_Space (Val : Net.RFLX_Types.Base_Integer) return Boolean is
     (Val in 1 | 36 | 256);

   function To_Base_Integer (Enum : Net.ARP.Hardware_Address_Space) return Net.RFLX_Types.Base_Integer is
     (case Enum is
          when HAS_ETHERNET =>
             1,
          when HAS_HW_EXP1 =>
             36,
          when HAS_HW_EXP2 =>
             256);

   pragma Warnings (Off, "unreachable branch");

   function To_Actual (Val : Net.RFLX_Types.Base_Integer) return Net.ARP.Hardware_Address_Space is
     (case Val is
          when 1 =>
             HAS_ETHERNET,
          when 36 =>
             HAS_HW_EXP1,
          when 256 =>
             HAS_HW_EXP2,
          when others =>
             Net.ARP.Hardware_Address_Space'Last)
   with
     Pre =>
       Valid_Hardware_Address_Space (Val);

   pragma Warnings (On, "unreachable branch");

   type Opcode is (OP_REQUEST, OP_REPLY)
   with
     Size =>
       16;
   for Opcode use (OP_REQUEST => 1, OP_REPLY => 2);

   function Valid_Opcode (Val : Net.RFLX_Types.Base_Integer) return Boolean is
     (Val in 1 | 2);

   function To_Base_Integer (Enum : Net.ARP.Opcode) return Net.RFLX_Types.Base_Integer is
     (case Enum is
          when OP_REQUEST =>
             1,
          when OP_REPLY =>
             2);

   pragma Warnings (Off, "unreachable branch");

   function To_Actual (Val : Net.RFLX_Types.Base_Integer) return Net.ARP.Opcode is
     (case Val is
          when 1 =>
             OP_REQUEST,
          when 2 =>
             OP_REPLY,
          when others =>
             Net.ARP.Opcode'Last)
   with
     Pre =>
       Valid_Opcode (Val);

   pragma Warnings (On, "unreachable branch");

   type Address_Length is range 0 .. 2**8 - 1
   with
     Size =>
       8;

   function Valid_Address_Length (Val : Net.RFLX_Types.Base_Integer) return Boolean is
     (Val <= 255);

   function To_Base_Integer (Val : Net.ARP.Address_Length) return Net.RFLX_Types.Base_Integer is
     (Net.RFLX_Types.Base_Integer (Val));

   function To_Actual (Val : Net.RFLX_Types.Base_Integer) return Net.ARP.Address_Length is
     (Net.ARP.Address_Length (Val))
   with
     Pre =>
       Valid_Address_Length (Val);

end Net.ARP;
