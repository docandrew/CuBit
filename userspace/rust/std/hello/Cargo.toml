[package]
name = "rust-std-hello"
version = "0.1.0"
edition = "2024"
publish = false

# Built with the patched std for x86_64-unknown-cubit, outside the no_std
# workspace (see ../README.md).
[workspace]

[profile.release]
panic = "abort"
opt-level = 2
debug = 1
