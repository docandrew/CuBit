[package]
name = "cubit-rust-probe"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["staticlib"]
path = "src/lib.rs"

[dependencies]
cubit = { path = "../cubit" }
cubit-allocator = { path = "../allocator" }
