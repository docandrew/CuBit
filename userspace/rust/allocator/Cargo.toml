[package]
name = "cubit-allocator"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
path = "src/lib.rs"

[target.'cfg(any(target_os = "none", target_os = "cubit"))'.dependencies]
cubit = { path = "../cubit" }
